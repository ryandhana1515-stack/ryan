import { workflow, node, trigger, sticky, expr, ifElse } from '@n8n/workflow-sdk';

const whenCalled = trigger({
  type: 'n8n-nodes-base.executeWorkflowTrigger',
  version: 1.2,
  config: { name: 'When Called by Lead Intake', parameters: { inputSource: 'workflowInputs', workflowInputs: { values: [{"name":"tenant_id","type":"string"},{"name":"lead_id","type":"string"},{"name":"contact_name","type":"string"},{"name":"company_name","type":"string"},{"name":"industry","type":"string"},{"name":"email","type":"string"},{"name":"phone","type":"string"},{"name":"channel","type":"string"},{"name":"message","type":"string"},{"name":"conversation_json","type":"string"},{"name":"sales_summary","type":"string"},{"name":"extracted_json","type":"string"},{"name":"test_mode","type":"boolean"},{"name":"notify_email","type":"string"},{"name":"source_execution_id","type":"string"}] } }, position: [0, 300] },
  output: [{ tenant_id: 'fusiontech', lead_id: 'lead_x', contact_name: 'Marcus Lim', company_name: 'Prestige Motors', industry: 'car dealership', email: 'm@example.com', phone: '', channel: 'whatsapp', message: 'I want a mock-up for prestigemotors.sg', conversation_json: '[]', sales_summary: 'Wants test-drive bookings', extracted_json: '{}', test_mode: true, notify_email: 'owner@example.com', source_execution_id: '1' }]
});

const plan = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: { name: 'Plan Research', parameters: { mode: 'runOnceForAllItems', language: 'javaScript', jsCode: "var WR_VERSION = 'website-intelligence-1.0.0';\nvar WR_SOCIAL_HOSTS = ['facebook.com', 'instagram.com', 'linkedin.com', 'tiktok.com', 'youtube.com', 'x.com', 'twitter.com'];\nvar WR_DIRECTORY_HOSTS = ['google.com', 'maps.google', 'yelp.com', 'tripadvisor', 'wikipedia.org', 'yellowpages', 'sgpbusiness', 'recordowl', 'streetdirectory', 'carousell', 'shopee', 'lazada', 'glassdoor', 'indeed', 'bing.com', 'duckduckgo', 'reddit.com', 'hardwarezone', 'mycareersfuture', 'acra.gov.sg', 'trustpilot', 'sgcarmart'];\nfunction wrStr(v, max) { if (v === undefined || v === null) return ''; var s = String(v).replace(/\\s+/g, ' ').trim(); return max && s.length > max ? s.slice(0, max) : s; }\nfunction wrArr(v, max) { if (!v) return []; if (!Array.isArray(v)) v = [v]; return v.map(function (x) { return typeof x === 'string' ? wrStr(x, 300) : (x && typeof x === 'object' ? wrStr(x.fact || x.text || x.name || JSON.stringify(x), 300) : wrStr(x, 300)); }).filter(Boolean).slice(0, max || 20); }\nfunction wrHost(url) { var m = /^(?:https?:\\/\\/)?(?:www\\.)?([^\\/?#:]+)/i.exec(String(url || '').trim()); return m ? m[1].toLowerCase() : ''; }\nfunction wrUrl(u) { u = wrStr(u, 300); if (!u) return ''; if (!/^https?:\\/\\//i.test(u)) u = 'https://' + u; return u.replace(/[)\\].,;]+$/, ''); }\nfunction wrFindUrls(text) {\n  var out = [], re = /\\b((?:https?:\\/\\/)?(?:www\\.)?[a-z0-9-]+(?:\\.[a-z0-9-]+)*\\.(?:com|sg|co|net|org|io|asia|shop|store|clinic|dental|my|au|uk|biz|info)(?:\\.[a-z]{2})?(?:\\/[^\\s\"'<>)]*)?)/gi, m;\n  text = String(text || '');\n  while ((m = re.exec(text)) !== null) { var u = m[1]; if (/@/.test(text.slice(Math.max(0, m.index - 1), m.index + 1))) continue; if (out.indexOf(u) === -1) out.push(u); }\n  return out;\n}\nfunction wrIsSocial(host) { return WR_SOCIAL_HOSTS.some(function (h) { return host === h || host.endsWith('.' + h); }); }\nfunction wrIsDirectory(host) { return WR_DIRECTORY_HOSTS.some(function (h) { return host.indexOf(h) !== -1; }); }\nfunction wrTokens(s) { return wrStr(s).toLowerCase().replace(/[^a-z0-9 ]/g, ' ').split(' ').filter(function (t) { return t.length > 2 && ['pte', 'ltd', 'llp', 'the', 'and', 'singapore', 'company', 'co', 'inc', 'llc', 'sdn', 'bhd'].indexOf(t) === -1; }); }\n/** John's hand-off → normalized input (same fields as the Website Builder + the website John or the customer mentioned). */\nfunction wrInput(inp) {\n  inp = (inp && typeof inp === 'object') ? inp : {};\n  var conversation = []; try { conversation = JSON.parse(inp.conversation_json || '[]'); } catch (e) { conversation = []; }\n  if (!Array.isArray(conversation)) conversation = [];\n  var extracted = {}; try { extracted = JSON.parse(inp.extracted_json || '{}'); } catch (e) { extracted = {}; }\n  if (!extracted || typeof extracted !== 'object') extracted = {};\n  var text = [inp.message, inp.sales_summary, extracted.website, extracted.existing_website, extracted.company_website].concat(conversation.map(function (m) { return m && m.content; })).filter(Boolean).join('\\n');\n  var urls = wrFindUrls(wrStr(inp.website) ? inp.website + '\\n' + text : text).filter(function (u) { var h = wrHost(u); return h && !wrIsSocial(h) && !wrIsDirectory(h) && !/fusiontech|n8n\\.cloud|lovable|whatsapp|wa\\.me/i.test(h); });\n  var socials = wrFindUrls(text).filter(function (u) { return wrIsSocial(wrHost(u)); });\n  return {\n    tenant_id: wrStr(inp.tenant_id) || 'fusiontech', lead_id: wrStr(inp.lead_id) || ('lead_unknown_' + Date.now().toString(36)),\n    contact_name: wrStr(inp.contact_name, 120), company_name: wrStr(inp.company_name, 160) || wrStr(extracted.company_name, 160),\n    industry: wrStr(inp.industry, 120) || wrStr(extracted.industry, 120), email: wrStr(inp.email, 160), phone: wrStr(inp.phone, 40), channel: wrStr(inp.channel) || 'unknown',\n    message: wrStr(inp.message, 2000), conversation: conversation, sales_summary: wrStr(inp.sales_summary, 1500), extracted: extracted,\n    location: wrStr(extracted.location || extracted.city || extracted.country, 80) || 'Singapore',\n    website: urls.length ? wrUrl(urls[0]) : '', website_source: urls.length ? (wrStr(inp.website) && wrFindUrls(inp.website)[0] === urls[0] ? 'john' : 'customer_words') : '', socials: socials.slice(0, 5),\n    test_mode: inp.test_mode === true || inp.test_mode === 'true', notify_email: wrStr(inp.notify_email, 160), source_execution_id: wrStr(inp.source_execution_id, 60)\n  };\n}\n/** The minimum research pass (Rule 4): queries for the company, its reviews, its service and the market. */\nfunction wrQueries(input) {\n  var name = input.company_name, loc = input.location || 'Singapore', svc = input.industry || '';\n  if (!name) return [];\n  var q = [\n    { key: 'name', query: name },\n    { key: 'name_location', query: name + ' ' + loc },\n    { key: 'reviews', query: name + ' reviews' }\n  ];\n  if (svc) { q.push({ key: 'name_service', query: name + ' ' + svc }); q.push({ key: 'market', query: svc + ' ' + loc }); q.push({ key: 'buyer_intent', query: 'best ' + svc + ' ' + loc }); }\n  return q;\n}\n/** Normalizes whatever shape the search node returns into [{query_key, title, url, snippet}]. */\n// ---- n8n glue: one item per search query; the normalized hand-off rides on every item ----\nconst inp = ($input.first() && $input.first().json) || {};\nconst input = wrInput(inp);\nconst queries = wrQueries(input);\nconst started_at = new Date().toISOString();\nif (!queries.length) return [{ json: { query_key: 'none', query: '', input, started_at, no_company: true } }];\nreturn queries.map((q) => ({ json: { query_key: q.key, query: q.query, input, started_at } }));\n" }, position: [220, 300] },
  output: [{ query_key: 'name', query: 'Prestige Motors', input: { company_name: 'Prestige Motors', lead_id: 'lead_x', website: 'https://prestigemotors.sg', location: 'Singapore', industry: 'car dealership', test_mode: true }, started_at: '2026-01-01T00:00:00.000Z' }]
});

const search = node({
  type: 'n8n-nodes-browserbase.browserbase',
  version: 3,
  config: { name: 'Web Search', onError: 'continueRegularOutput', alwaysOutputData: true, parameters: { resource: 'search', operation: 'search', query: expr('{{ $json.query }}'), numResults: 8 }, position: [440, 300] },
  output: [{ results: [{ title: 'Prestige Motors', url: 'https://prestigemotors.sg', description: 'BMW showroom' }] }]
});

const identify = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: { name: 'Identify Company', parameters: { mode: 'runOnceForAllItems', language: 'javaScript', jsCode: "var WR_VERSION = 'website-intelligence-1.0.0';\nvar WR_SOCIAL_HOSTS = ['facebook.com', 'instagram.com', 'linkedin.com', 'tiktok.com', 'youtube.com', 'x.com', 'twitter.com'];\nvar WR_DIRECTORY_HOSTS = ['google.com', 'maps.google', 'yelp.com', 'tripadvisor', 'wikipedia.org', 'yellowpages', 'sgpbusiness', 'recordowl', 'streetdirectory', 'carousell', 'shopee', 'lazada', 'glassdoor', 'indeed', 'bing.com', 'duckduckgo', 'reddit.com', 'hardwarezone', 'mycareersfuture', 'acra.gov.sg', 'trustpilot', 'sgcarmart'];\nfunction wrStr(v, max) { if (v === undefined || v === null) return ''; var s = String(v).replace(/\\s+/g, ' ').trim(); return max && s.length > max ? s.slice(0, max) : s; }\nfunction wrArr(v, max) { if (!v) return []; if (!Array.isArray(v)) v = [v]; return v.map(function (x) { return typeof x === 'string' ? wrStr(x, 300) : (x && typeof x === 'object' ? wrStr(x.fact || x.text || x.name || JSON.stringify(x), 300) : wrStr(x, 300)); }).filter(Boolean).slice(0, max || 20); }\nfunction wrHost(url) { var m = /^(?:https?:\\/\\/)?(?:www\\.)?([^\\/?#:]+)/i.exec(String(url || '').trim()); return m ? m[1].toLowerCase() : ''; }\nfunction wrUrl(u) { u = wrStr(u, 300); if (!u) return ''; if (!/^https?:\\/\\//i.test(u)) u = 'https://' + u; return u.replace(/[)\\].,;]+$/, ''); }\nfunction wrFindUrls(text) {\n  var out = [], re = /\\b((?:https?:\\/\\/)?(?:www\\.)?[a-z0-9-]+(?:\\.[a-z0-9-]+)*\\.(?:com|sg|co|net|org|io|asia|shop|store|clinic|dental|my|au|uk|biz|info)(?:\\.[a-z]{2})?(?:\\/[^\\s\"'<>)]*)?)/gi, m;\n  text = String(text || '');\n  while ((m = re.exec(text)) !== null) { var u = m[1]; if (/@/.test(text.slice(Math.max(0, m.index - 1), m.index + 1))) continue; if (out.indexOf(u) === -1) out.push(u); }\n  return out;\n}\nfunction wrIsSocial(host) { return WR_SOCIAL_HOSTS.some(function (h) { return host === h || host.endsWith('.' + h); }); }\nfunction wrIsDirectory(host) { return WR_DIRECTORY_HOSTS.some(function (h) { return host.indexOf(h) !== -1; }); }\nfunction wrTokens(s) { return wrStr(s).toLowerCase().replace(/[^a-z0-9 ]/g, ' ').split(' ').filter(function (t) { return t.length > 2 && ['pte', 'ltd', 'llp', 'the', 'and', 'singapore', 'company', 'co', 'inc', 'llc', 'sdn', 'bhd'].indexOf(t) === -1; }); }\n/** John's hand-off → normalized input (same fields as the Website Builder + the website John or the customer mentioned). */\nfunction wrSearchItems(items) {\n  var out = [];\n  (items || []).forEach(function (it) {\n    var j = it && it.json ? it.json : it; if (!j || typeof j !== 'object') return;\n    var key = wrStr(j.query_key || j.key || (j.__query && j.__query.key));\n    var list = null;\n    ['results', 'organic', 'web', 'items', 'data', 'hits'].forEach(function (k) { if (!list && Array.isArray(j[k])) list = j[k]; });\n    if (!list && j.results && Array.isArray(j.results.web)) list = j.results.web;\n    if (!list && (j.url || j.link)) list = [j];\n    (list || []).forEach(function (r) {\n      if (!r || typeof r !== 'object') return;\n      var url = wrStr(r.url || r.link || r.href, 400); if (!url) return;\n      out.push({ query_key: key, title: wrStr(r.title || r.name, 200), url: url, snippet: wrStr(r.snippet || r.description || r.content || r.text, 400) });\n    });\n  });\n  return out;\n}\n/** Rule 3: which company is this? John's/the customer's URL wins; otherwise the best-matching official site from the name queries. */\nfunction wrIdentify(input, results) {\n  var out = { website: '', host: '', confidence: 'low', reason: '', candidates: [], socials: input.socials.slice() };\n  if (input.website) { out.website = input.website; out.host = wrHost(input.website); out.confidence = 'high'; out.reason = 'website given by ' + (input.website_source === 'john' ? 'John' : 'the customer'); }\n  var toks = wrTokens(input.company_name);\n  var scored = {};\n  (results || []).forEach(function (r) {\n    if (['name', 'name_location', 'name_service'].indexOf(r.query_key) === -1 && r.query_key) return;\n    var host = wrHost(r.url); if (!host) return;\n    if (wrIsSocial(host)) { if (out.socials.indexOf(r.url) === -1 && out.socials.length < 5) out.socials.push(r.url); return; }\n    if (wrIsDirectory(host)) return;\n    var hay = (r.title + ' ' + r.snippet + ' ' + host).toLowerCase();\n    var score = toks.reduce(function (s, t) { return s + (hay.indexOf(t) !== -1 ? 1 : 0); }, 0) + (toks.length && host.replace(/[^a-z0-9]/g, '').indexOf(toks.join('')) !== -1 ? 2 : 0);\n    if (!scored[host] || scored[host].score < score) scored[host] = { host: host, url: r.url, title: r.title, score: score };\n  });\n  var cands = Object.keys(scored).map(function (h) { return scored[h]; }).filter(function (c) { return c.score > 0; }).sort(function (a, b) { return b.score - a.score; });\n  out.candidates = cands.slice(0, 5);\n  if (!out.website) {\n    if (cands.length && (cands.length === 1 || cands[0].score > (cands[1] ? cands[1].score : 0))) {\n      var top = cands[0]; var strong = top.score >= Math.max(2, toks.length);\n      out.website = wrUrl('https://' + top.host); out.host = top.host; out.confidence = strong ? 'medium' : 'low';\n      out.reason = strong ? 'search results point to one site matching the name' : 'one weak candidate from search; treat facts as unverified';\n    } else if (cands.length > 1) { out.reason = 'several businesses match this name: ' + cands.slice(0, 3).map(function (c) { return c.host; }).join(', '); }\n    else out.reason = 'no matching official website found in search';\n  }\n  return out;\n}\n/** HTML → plain text + structure (title, description, h1s, links, contact signals). */\n// ---- n8n glue: pair each search result item with its query, identify the company, choose the homepage ----\nconst plan = $('Plan Research').all().map((i) => i.json);\nconst input = plan[0].input;\nconst raw = $input.all();\nconst tagged = raw.map((it, i) => {\n  let idx = i;\n  try { const p = it.pairedItem; if (p !== undefined && p !== null) idx = Array.isArray(p) ? (p[0].item ?? i) : (typeof p === 'object' ? (p.item ?? i) : p); } catch (e) { idx = i; }\n  const q = plan[Math.min(idx, plan.length - 1)] || {};\n  const json = (it && it.json) || {};\n  return { json: Object.assign({}, json, { query_key: json.query_key || q.query_key, __query: q.query }) };\n});\nconst results = wrSearchItems(tagged);\nconst searchErrors = raw.filter((it) => it && it.json && it.json.error).length;\nconst identity = wrIdentify(input, results);\nreturn [{ json: { input, identity, results, search_errors: searchErrors, search_items: raw.length, homepage_url: identity.website || '', started_at: plan[0].started_at } }];\n" }, position: [660, 300] },
  output: [{ input: { company_name: 'Prestige Motors', lead_id: 'lead_x' }, identity: { website: 'https://prestigemotors.sg', host: 'prestigemotors.sg', confidence: 'high', reason: 'given', candidates: [], socials: [] }, results: [], search_errors: 0, search_items: 6, homepage_url: 'https://prestigemotors.sg', started_at: '2026-01-01T00:00:00.000Z' }]
});

const fetchHome = node({
  type: 'n8n-nodes-browserbase.browserbase',
  version: 3,
  config: { name: 'Fetch Homepage', onError: 'continueRegularOutput', alwaysOutputData: true, parameters: { resource: 'fetch', operation: 'fetch', fetchUrl: expr('{{ $json.homepage_url }}'), fetchOptions: { allowRedirects: true } }, position: [880, 300] },
  output: [{ html: '<html><title>Prestige Motors</title></html>', status: 200 }]
});

const pickPages = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: { name: 'Pick Pages', parameters: { mode: 'runOnceForAllItems', language: 'javaScript', jsCode: "var WR_VERSION = 'website-intelligence-1.0.0';\nvar WR_MAX_PAGE_TEXT = 3500;\nvar WR_SOCIAL_HOSTS = ['facebook.com', 'instagram.com', 'linkedin.com', 'tiktok.com', 'youtube.com', 'x.com', 'twitter.com'];\nvar WR_DIRECTORY_HOSTS = ['google.com', 'maps.google', 'yelp.com', 'tripadvisor', 'wikipedia.org', 'yellowpages', 'sgpbusiness', 'recordowl', 'streetdirectory', 'carousell', 'shopee', 'lazada', 'glassdoor', 'indeed', 'bing.com', 'duckduckgo', 'reddit.com', 'hardwarezone', 'mycareersfuture', 'acra.gov.sg', 'trustpilot', 'sgcarmart'];\nvar WR_PAGE_RE = /(about|service|product|treatment|contact|faq|pricing|price|package|booking|book|project|portfolio|case|testimonial|review|menu|shop|gallery|location)/i;\nfunction wrStr(v, max) { if (v === undefined || v === null) return ''; var s = String(v).replace(/\\s+/g, ' ').trim(); return max && s.length > max ? s.slice(0, max) : s; }\nfunction wrArr(v, max) { if (!v) return []; if (!Array.isArray(v)) v = [v]; return v.map(function (x) { return typeof x === 'string' ? wrStr(x, 300) : (x && typeof x === 'object' ? wrStr(x.fact || x.text || x.name || JSON.stringify(x), 300) : wrStr(x, 300)); }).filter(Boolean).slice(0, max || 20); }\nfunction wrHost(url) { var m = /^(?:https?:\\/\\/)?(?:www\\.)?([^\\/?#:]+)/i.exec(String(url || '').trim()); return m ? m[1].toLowerCase() : ''; }\nfunction wrUrl(u) { u = wrStr(u, 300); if (!u) return ''; if (!/^https?:\\/\\//i.test(u)) u = 'https://' + u; return u.replace(/[)\\].,;]+$/, ''); }\nfunction wrFindUrls(text) {\n  var out = [], re = /\\b((?:https?:\\/\\/)?(?:www\\.)?[a-z0-9-]+(?:\\.[a-z0-9-]+)*\\.(?:com|sg|co|net|org|io|asia|shop|store|clinic|dental|my|au|uk|biz|info)(?:\\.[a-z]{2})?(?:\\/[^\\s\"'<>)]*)?)/gi, m;\n  text = String(text || '');\n  while ((m = re.exec(text)) !== null) { var u = m[1]; if (/@/.test(text.slice(Math.max(0, m.index - 1), m.index + 1))) continue; if (out.indexOf(u) === -1) out.push(u); }\n  return out;\n}\nfunction wrIsSocial(host) { return WR_SOCIAL_HOSTS.some(function (h) { return host === h || host.endsWith('.' + h); }); }\nfunction wrIsDirectory(host) { return WR_DIRECTORY_HOSTS.some(function (h) { return host.indexOf(h) !== -1; }); }\nfunction wrTokens(s) { return wrStr(s).toLowerCase().replace(/[^a-z0-9 ]/g, ' ').split(' ').filter(function (t) { return t.length > 2 && ['pte', 'ltd', 'llp', 'the', 'and', 'singapore', 'company', 'co', 'inc', 'llc', 'sdn', 'bhd'].indexOf(t) === -1; }); }\n/** John's hand-off → normalized input (same fields as the Website Builder + the website John or the customer mentioned). */\nfunction wrHtmlToText(html, baseUrl) {\n  html = String(html || '');\n  var get = function (re) { var m = re.exec(html); return m ? wrStr(m[1].replace(/<[^>]+>/g, ' '), 300) : ''; };\n  var title = get(/<title[^>]*>([\\s\\S]*?)<\\/title>/i);\n  var description = get(/<meta[^>]+name=[\"']description[\"'][^>]*content=[\"']([^\"']*)[\"']/i) || get(/<meta[^>]+content=[\"']([^\"']*)[\"'][^>]*name=[\"']description[\"']/i);\n  var h1s = []; var hre = /<h1[^>]*>([\\s\\S]*?)<\\/h1>/gi, m; while ((m = hre.exec(html)) !== null && h1s.length < 5) { var t = wrStr(m[1].replace(/<[^>]+>/g, ' '), 200); if (t) h1s.push(t); }\n  var links = []; var lre = /<a[^>]+href=[\"']([^\"'#]+)[\"'][^>]*>([\\s\\S]*?)<\\/a>/gi; var base = wrUrl(baseUrl); var host = wrHost(base);\n  while ((m = lre.exec(html)) !== null && links.length < 200) {\n    var href = m[1].trim(), text = wrStr(m[2].replace(/<[^>]+>/g, ' '), 80);\n    if (/^(mailto:|tel:|javascript:)/i.test(href)) continue;\n    if (/^\\//.test(href)) href = base.replace(/\\/+$/, '') + href; else if (!/^https?:\\/\\//i.test(href)) href = base.replace(/\\/+$/, '') + '/' + href.replace(/^\\.?\\//, '');\n    if (wrHost(href) !== host) continue;\n    links.push({ url: href.split('?')[0], text: text });\n  }\n  var body = html.replace(/<script[\\s\\S]*?<\\/script>/gi, ' ').replace(/<style[\\s\\S]*?<\\/style>/gi, ' ').replace(/<noscript[\\s\\S]*?<\\/noscript>/gi, ' ').replace(/<!--[\\s\\S]*?-->/g, ' ').replace(/<\\/(p|div|li|h[1-6]|section|article|tr|br)>/gi, '\\n').replace(/<[^>]+>/g, ' ').replace(/&nbsp;/g, ' ').replace(/&amp;/g, '&').replace(/&quot;/g, '\"').replace(/&#39;|&apos;/g, \"'\").replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/[ \\t]+/g, ' ').replace(/\\s*\\n\\s*/g, '\\n').trim();\n  var lower = html.toLowerCase();\n  return {\n    title: title, description: description, h1s: h1s, links: links, text: body.slice(0, WR_MAX_PAGE_TEXT), length: body.length,\n    signals: { whatsapp: /wa\\.me|whatsapp/i.test(lower), booking: /book(ing)?|appointment|calendly|reserve/i.test(lower), form: /<form/i.test(lower), phone: /tel:/i.test(lower) || /\\+65\\s?\\d{4}\\s?\\d{4}|\\b[689]\\d{3}\\s?\\d{4}\\b/.test(body), email: /mailto:/i.test(lower), shop: /add to cart|checkout|shopify|woocommerce/i.test(lower), analytics: /gtag\\(|googletagmanager|fbq\\(|facebook\\.net\\/en_us\\/fbevents/i.test(lower) },\n    phones: (body.match(/(?:\\+65[\\s-]?)?[689]\\d{3}[\\s-]?\\d{4}\\b/g) || []).slice(0, 3), emails: (body.match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,}/gi) || []).slice(0, 3)\n  };\n}\n/** Up to 3 more pages worth reading (Rule 5). */\nfunction wrPickPages(links, max) {\n  var seen = {}, out = [];\n  (links || []).forEach(function (l) { if (out.length >= (max || 3)) return; var path = l.url.replace(/^https?:\\/\\/[^\\/]+/i, ''); if (!path || path === '/' || seen[l.url]) return; if (WR_PAGE_RE.test(path) || WR_PAGE_RE.test(l.text)) { seen[l.url] = true; out.push(l.url); } });\n  return out;\n}\n/** Normalizes a fetched page item (any shape) into { url, ok, html }. */\nfunction wrFetchedPage(item, url) {\n  var j = item && item.json ? item.json : (item || {});\n  if (j && j.error) return { url: url, ok: false, html: '', error: wrStr(j.error.message || j.error, 200) };\n  var html = '';\n  ['html', 'content', 'text', 'body', 'data', 'markdown'].forEach(function (k) { if (!html && typeof j[k] === 'string' && j[k].length > html.length) html = j[k]; });\n  if (!html && j.response && typeof j.response === 'object') ['html', 'content', 'text', 'body'].forEach(function (k) { if (!html && typeof j.response[k] === 'string') html = j.response[k]; });\n  if (!html) { var best = ''; Object.keys(j).forEach(function (k) { if (typeof j[k] === 'string' && j[k].length > best.length) best = j[k]; }); if (best.length > 200) html = best; }\n  return { url: url || wrStr(j.url, 300), ok: html.length > 0, html: html, status: j.status || j.statusCode || null };\n}\n/** Everything research found, labelled (Rule 7) and boiled down to a digest the model and the fallback can use. */\n// ---- n8n glue: read the homepage, pick up to 3 internal pages worth fetching (Rule 5) ----\nconst id = $('Identify Company').first().json;\nconst page = wrFetchedPage($input.first(), id.homepage_url);\nlet urls = [];\nif (page.ok) { const parsed = wrHtmlToText(page.html, id.homepage_url); urls = wrPickPages(parsed.links, 3); }\nif (!urls.length) return [{ json: { url: '', skip: true, homepage_ok: page.ok } }];\nreturn urls.map((u) => ({ json: { url: u, skip: false, homepage_ok: page.ok } }));\n" }, position: [1100, 300] },
  output: [{ url: 'https://prestigemotors.sg/about', skip: false, homepage_ok: true }]
});

const fetchPages = node({
  type: 'n8n-nodes-browserbase.browserbase',
  version: 3,
  config: { name: 'Fetch Pages', onError: 'continueRegularOutput', alwaysOutputData: true, parameters: { resource: 'fetch', operation: 'fetch', fetchUrl: expr('{{ $json.url }}'), fetchOptions: { allowRedirects: true } }, position: [1320, 300] },
  output: [{ html: '<html><title>About</title></html>', status: 200 }]
});

const digest = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: { name: 'Digest Research', parameters: { mode: 'runOnceForAllItems', language: 'javaScript', jsCode: "var WR_VERSION = 'website-intelligence-1.0.0';\nvar WR_MAX_DIGEST = 9000;\nvar WR_MAX_PAGE_TEXT = 3500;\nvar WR_SOCIAL_HOSTS = ['facebook.com', 'instagram.com', 'linkedin.com', 'tiktok.com', 'youtube.com', 'x.com', 'twitter.com'];\nvar WR_DIRECTORY_HOSTS = ['google.com', 'maps.google', 'yelp.com', 'tripadvisor', 'wikipedia.org', 'yellowpages', 'sgpbusiness', 'recordowl', 'streetdirectory', 'carousell', 'shopee', 'lazada', 'glassdoor', 'indeed', 'bing.com', 'duckduckgo', 'reddit.com', 'hardwarezone', 'mycareersfuture', 'acra.gov.sg', 'trustpilot', 'sgcarmart'];\nvar WR_PAGE_RE = /(about|service|product|treatment|contact|faq|pricing|price|package|booking|book|project|portfolio|case|testimonial|review|menu|shop|gallery|location)/i;\nfunction wrStr(v, max) { if (v === undefined || v === null) return ''; var s = String(v).replace(/\\s+/g, ' ').trim(); return max && s.length > max ? s.slice(0, max) : s; }\nfunction wrArr(v, max) { if (!v) return []; if (!Array.isArray(v)) v = [v]; return v.map(function (x) { return typeof x === 'string' ? wrStr(x, 300) : (x && typeof x === 'object' ? wrStr(x.fact || x.text || x.name || JSON.stringify(x), 300) : wrStr(x, 300)); }).filter(Boolean).slice(0, max || 20); }\nfunction wrHost(url) { var m = /^(?:https?:\\/\\/)?(?:www\\.)?([^\\/?#:]+)/i.exec(String(url || '').trim()); return m ? m[1].toLowerCase() : ''; }\nfunction wrUrl(u) { u = wrStr(u, 300); if (!u) return ''; if (!/^https?:\\/\\//i.test(u)) u = 'https://' + u; return u.replace(/[)\\].,;]+$/, ''); }\nfunction wrFindUrls(text) {\n  var out = [], re = /\\b((?:https?:\\/\\/)?(?:www\\.)?[a-z0-9-]+(?:\\.[a-z0-9-]+)*\\.(?:com|sg|co|net|org|io|asia|shop|store|clinic|dental|my|au|uk|biz|info)(?:\\.[a-z]{2})?(?:\\/[^\\s\"'<>)]*)?)/gi, m;\n  text = String(text || '');\n  while ((m = re.exec(text)) !== null) { var u = m[1]; if (/@/.test(text.slice(Math.max(0, m.index - 1), m.index + 1))) continue; if (out.indexOf(u) === -1) out.push(u); }\n  return out;\n}\nfunction wrIsSocial(host) { return WR_SOCIAL_HOSTS.some(function (h) { return host === h || host.endsWith('.' + h); }); }\nfunction wrIsDirectory(host) { return WR_DIRECTORY_HOSTS.some(function (h) { return host.indexOf(h) !== -1; }); }\nfunction wrTokens(s) { return wrStr(s).toLowerCase().replace(/[^a-z0-9 ]/g, ' ').split(' ').filter(function (t) { return t.length > 2 && ['pte', 'ltd', 'llp', 'the', 'and', 'singapore', 'company', 'co', 'inc', 'llc', 'sdn', 'bhd'].indexOf(t) === -1; }); }\n/** John's hand-off → normalized input (same fields as the Website Builder + the website John or the customer mentioned). */\nfunction wrHtmlToText(html, baseUrl) {\n  html = String(html || '');\n  var get = function (re) { var m = re.exec(html); return m ? wrStr(m[1].replace(/<[^>]+>/g, ' '), 300) : ''; };\n  var title = get(/<title[^>]*>([\\s\\S]*?)<\\/title>/i);\n  var description = get(/<meta[^>]+name=[\"']description[\"'][^>]*content=[\"']([^\"']*)[\"']/i) || get(/<meta[^>]+content=[\"']([^\"']*)[\"'][^>]*name=[\"']description[\"']/i);\n  var h1s = []; var hre = /<h1[^>]*>([\\s\\S]*?)<\\/h1>/gi, m; while ((m = hre.exec(html)) !== null && h1s.length < 5) { var t = wrStr(m[1].replace(/<[^>]+>/g, ' '), 200); if (t) h1s.push(t); }\n  var links = []; var lre = /<a[^>]+href=[\"']([^\"'#]+)[\"'][^>]*>([\\s\\S]*?)<\\/a>/gi; var base = wrUrl(baseUrl); var host = wrHost(base);\n  while ((m = lre.exec(html)) !== null && links.length < 200) {\n    var href = m[1].trim(), text = wrStr(m[2].replace(/<[^>]+>/g, ' '), 80);\n    if (/^(mailto:|tel:|javascript:)/i.test(href)) continue;\n    if (/^\\//.test(href)) href = base.replace(/\\/+$/, '') + href; else if (!/^https?:\\/\\//i.test(href)) href = base.replace(/\\/+$/, '') + '/' + href.replace(/^\\.?\\//, '');\n    if (wrHost(href) !== host) continue;\n    links.push({ url: href.split('?')[0], text: text });\n  }\n  var body = html.replace(/<script[\\s\\S]*?<\\/script>/gi, ' ').replace(/<style[\\s\\S]*?<\\/style>/gi, ' ').replace(/<noscript[\\s\\S]*?<\\/noscript>/gi, ' ').replace(/<!--[\\s\\S]*?-->/g, ' ').replace(/<\\/(p|div|li|h[1-6]|section|article|tr|br)>/gi, '\\n').replace(/<[^>]+>/g, ' ').replace(/&nbsp;/g, ' ').replace(/&amp;/g, '&').replace(/&quot;/g, '\"').replace(/&#39;|&apos;/g, \"'\").replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/[ \\t]+/g, ' ').replace(/\\s*\\n\\s*/g, '\\n').trim();\n  var lower = html.toLowerCase();\n  return {\n    title: title, description: description, h1s: h1s, links: links, text: body.slice(0, WR_MAX_PAGE_TEXT), length: body.length,\n    signals: { whatsapp: /wa\\.me|whatsapp/i.test(lower), booking: /book(ing)?|appointment|calendly|reserve/i.test(lower), form: /<form/i.test(lower), phone: /tel:/i.test(lower) || /\\+65\\s?\\d{4}\\s?\\d{4}|\\b[689]\\d{3}\\s?\\d{4}\\b/.test(body), email: /mailto:/i.test(lower), shop: /add to cart|checkout|shopify|woocommerce/i.test(lower), analytics: /gtag\\(|googletagmanager|fbq\\(|facebook\\.net\\/en_us\\/fbevents/i.test(lower) },\n    phones: (body.match(/(?:\\+65[\\s-]?)?[689]\\d{3}[\\s-]?\\d{4}\\b/g) || []).slice(0, 3), emails: (body.match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,}/gi) || []).slice(0, 3)\n  };\n}\n/** Up to 3 more pages worth reading (Rule 5). */\nfunction wrFetchedPage(item, url) {\n  var j = item && item.json ? item.json : (item || {});\n  if (j && j.error) return { url: url, ok: false, html: '', error: wrStr(j.error.message || j.error, 200) };\n  var html = '';\n  ['html', 'content', 'text', 'body', 'data', 'markdown'].forEach(function (k) { if (!html && typeof j[k] === 'string' && j[k].length > html.length) html = j[k]; });\n  if (!html && j.response && typeof j.response === 'object') ['html', 'content', 'text', 'body'].forEach(function (k) { if (!html && typeof j.response[k] === 'string') html = j.response[k]; });\n  if (!html) { var best = ''; Object.keys(j).forEach(function (k) { if (typeof j[k] === 'string' && j[k].length > best.length) best = j[k]; }); if (best.length > 200) html = best; }\n  return { url: url || wrStr(j.url, 300), ok: html.length > 0, html: html, status: j.status || j.statusCode || null };\n}\n/** Everything research found, labelled (Rule 7) and boiled down to a digest the model and the fallback can use. */\nfunction wrDigest(o) {\n  var input = o.input, identity = o.identity, results = o.results || [], pages = o.pages || [];\n  var facts = [];\n  var push = function (fact, label, source) { fact = wrStr(fact, 300); if (fact && !facts.some(function (f) { return f.fact === fact; })) facts.push({ fact: fact, label: label, source: wrStr(source, 200) }); };\n  if (input.company_name) push('Company name: ' + input.company_name, 'JOHN_OR_CUSTOMER_PROVIDED_FACT', 'John hand-off');\n  if (input.industry) push('Industry: ' + input.industry, 'JOHN_OR_CUSTOMER_PROVIDED_FACT', 'John hand-off');\n  if (input.contact_name) push('Contact: ' + input.contact_name, 'JOHN_OR_CUSTOMER_PROVIDED_FACT', 'John hand-off');\n  if (input.sales_summary) push('John\\'s summary: ' + input.sales_summary, 'JOHN_OR_CUSTOMER_PROVIDED_FACT', 'John hand-off');\n  if (input.message) push('Customer said: ' + input.message, 'JOHN_OR_CUSTOMER_PROVIDED_FACT', 'customer message');\n  Object.keys(input.extracted || {}).forEach(function (k) { var v = input.extracted[k]; if (v && typeof v !== 'object' && ['company_name', 'industry'].indexOf(k) === -1) push(k.replace(/_/g, ' ') + ': ' + v, 'JOHN_OR_CUSTOMER_PROVIDED_FACT', 'John extracted'); });\n  var verifiedLabel = identity.confidence === 'high' ? 'VERIFIED_PUBLIC_FACT' : (identity.confidence === 'medium' ? 'VERIFIED_PUBLIC_FACT (site identified from search; confirm)' : 'THIRD_PARTY_PUBLIC_INFORMATION');\n  var site = { website: identity.website, pages_read: [], pages_failed: [], title: '', description: '', h1s: [], services_guess: [], signals: {}, phones: [], emails: [] };\n  var pageTexts = [];\n  pages.forEach(function (p) {\n    if (!p.ok) { site.pages_failed.push(p.url + (p.error ? ' (' + p.error + ')' : '')); return; }\n    var parsed = p.parsed || wrHtmlToText(p.html, p.url);\n    site.pages_read.push(p.url);\n    if (!site.title && parsed.title) site.title = parsed.title;\n    if (!site.description && parsed.description) site.description = parsed.description;\n    parsed.h1s.forEach(function (h) { if (site.h1s.indexOf(h) === -1 && site.h1s.length < 8) site.h1s.push(h); });\n    Object.keys(parsed.signals).forEach(function (k) { site.signals[k] = site.signals[k] || parsed.signals[k]; });\n    parsed.phones.forEach(function (x) { if (site.phones.indexOf(x) === -1) site.phones.push(x); });\n    parsed.emails.forEach(function (x) { if (site.emails.indexOf(x) === -1) site.emails.push(x); });\n    pageTexts.push('--- ' + p.url + ' ---\\n' + parsed.text);\n  });\n  if (identity.confidence !== 'low' && site.website) {\n    push('Official website: ' + site.website, verifiedLabel, site.website);\n    if (site.title) push('Site title: ' + site.title, verifiedLabel, site.website);\n    if (site.description) push('Site description: ' + site.description, verifiedLabel, site.website);\n    site.h1s.slice(0, 4).forEach(function (h) { push('Headline on site: ' + h, verifiedLabel, site.website); });\n    if (site.phones.length) push('Public phone: ' + site.phones.join(', '), verifiedLabel, site.website);\n    if (site.emails.length) push('Public email: ' + site.emails.join(', '), verifiedLabel, site.website);\n    ['whatsapp', 'booking', 'form', 'shop'].forEach(function (k) { if (site.signals[k]) push('Current site has ' + k + ' (' + (k === 'form' ? 'contact form' : k) + ')', verifiedLabel, site.website); });\n    if (site.pages_read.length && !site.signals.analytics) push('No analytics tag detected on the pages read (GA4/Meta pixel)', 'INFERENCE', site.website);\n  }\n  var host = identity.host;\n  var competitors = [], reviews = [], market = [];\n  results.forEach(function (r) {\n    var h = wrHost(r.url); if (!h) return;\n    if (r.query_key === 'reviews' && (r.snippet || r.title)) { if (reviews.length < 6) reviews.push({ source: r.url, text: wrStr(r.title + ' — ' + r.snippet, 300) }); return; }\n    if ((r.query_key === 'market' || r.query_key === 'buyer_intent') && h !== host && !wrIsSocial(h) && !wrIsDirectory(h)) { if (!competitors.some(function (c) { return wrHost(c.url) === h; }) && competitors.length < 5) competitors.push({ name: r.title, url: r.url, snippet: r.snippet }); return; }\n    if ((r.query_key === 'market' || r.query_key === 'buyer_intent') && wrIsDirectory(h) && market.length < 4) market.push(wrStr(r.title + ' — ' + r.snippet, 200));\n  });\n  reviews.forEach(function (rv) { push('Public mention: ' + rv.text, 'THIRD_PARTY_PUBLIC_INFORMATION', rv.source); });\n  identity.socials.forEach(function (s) { push('Official social profile (found in search): ' + s, 'THIRD_PARTY_PUBLIC_INFORMATION', s); });\n  var unknown = [];\n  ['testimonials', 'awards or certifications', 'years operating', 'team members', 'pricing', 'case studies or project photos'].forEach(function (k) { unknown.push(k + ' — [CLIENT TO PROVIDE]'); });\n  var digestText = ['IDENTITY: ' + (identity.website || 'no official website found') + ' | confidence ' + identity.confidence + ' | ' + identity.reason + (identity.candidates.length > 1 ? ' | other candidates: ' + identity.candidates.slice(1, 4).map(function (c) { return c.host; }).join(', ') : ''),\n    'FACT LEDGER:', facts.map(function (f) { return '- [' + f.label + '] ' + f.fact + ' (' + f.source + ')'; }).join('\\n'),\n    'PAGES READ: ' + (site.pages_read.join(', ') || 'none') + (site.pages_failed.length ? ' | could not read: ' + site.pages_failed.join(', ') : ''),\n    'SITE SIGNALS: ' + JSON.stringify(site.signals),\n    'COMPETITORS / ALTERNATIVES (from search, for strategy only, never copy): ' + (competitors.map(function (c) { return c.name + ' <' + c.url + '> ' + c.snippet; }).join(' || ') || 'none found'),\n    'MARKET SNIPPETS: ' + (market.join(' || ') || 'none'),\n    'PUBLIC REVIEWS / MENTIONS: ' + (reviews.map(function (r) { return r.text; }).join(' || ') || 'none found'),\n    'UNKNOWN (placeholders): ' + unknown.join('; '),\n    'PAGE TEXT:', pageTexts.join('\\n')].join('\\n');\n  return { version: WR_VERSION, identity: identity, facts: facts, site: site, competitors: competitors, reviews: reviews, market: market, unknown: unknown, digest_text: digestText.slice(0, WR_MAX_DIGEST) };\n}\n// ---- n8n glue: assemble every page + result into the labelled digest ----\nconst id = $('Identify Company').first().json;\nconst input = id.input;\nconst pages = [];\nconst home = wrFetchedPage($('Fetch Homepage').first(), id.homepage_url);\nif (id.homepage_url) pages.push(home);\nconst picks = $('Pick Pages').all().map((i) => i.json);\n$input.all().forEach((it, i) => { const p = picks[i] || {}; if (!p.url || p.skip) return; pages.push(wrFetchedPage(it, p.url)); });\nconst digest = wrDigest({ input, identity: id.identity, results: id.results, pages });\ndigest.search_items = id.search_items; digest.search_errors = id.search_errors;\nreturn [{ json: { input, identity: id.identity, digest, started_at: id.started_at } }];\n" }, position: [1540, 300] },
  output: [{ input: { company_name: 'Prestige Motors', lead_id: 'lead_x', test_mode: true }, identity: { website: 'https://prestigemotors.sg', confidence: 'high', reason: 'given' }, digest: { version: 'website-intelligence-1.0.0', facts: [], site: { pages_read: [], pages_failed: [], signals: {} }, competitors: [], reviews: [], unknown: [], digest_text: 'IDENTITY: …', search_items: 6, search_errors: 0 }, started_at: '2026-01-01T00:00:00.000Z' }]
});

const loadRole = node({
  type: 'n8n-nodes-base.github',
  version: 1.1,
  config: { name: 'Load Role Prompt', executeOnce: true, onError: 'continueRegularOutput', parameters: { authentication: 'oAuth2', resource: 'file', operation: 'get', owner: { __rl: true, mode: 'name', value: "ryandhana1515-stack" }, repository: { __rl: true, mode: 'name', value: "ryan" }, filePath: "zaphiel/vault/10_Agents/05a_Website_Intelligence — internal role prompt.md", asBinaryProperty: false, additionalParameters: {} }, credentials: { githubOAuth2Api: { id: "lZqYskCh7zVfXsc7", name: "GitHub account" } }, position: [1760, 300] },
  output: [{ content: 'IyBX', sha: 'x' }]
});

const compose = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: { name: 'Compose Research Prompt', parameters: { mode: 'runOnceForAllItems', language: 'javaScript', jsCode: "// Composes the Website Intelligence prompts AT RUN TIME: Ryan's role prompt (live from the vault) + the runtime addendum + the digest.\nconst ADDENDUM = \"# Runtime addendum (appended after Ryan's role prompt, which is read live from the vault)\\n\\nYou are running INSIDE the CEO Brain as an n8n step. John's hand-off and the research digest are in the user message. Everything you produce goes to the Website Creator and to John — never to the customer.\\n\\n## What you return\\nONE JSON object and nothing else (no prose, no markdown fences) with exactly these keys. Lists are arrays of short strings; unknown = [] or \\\"\\\", never a guess.\\n\\n{\\\"company_name\\\":\\\"\\\",\\\"company_url\\\":\\\"\\\",\\\"industry\\\":\\\"\\\",\\\"location\\\":\\\"\\\",\\\"business_summary\\\":\\\"\\\",\\\"verified_facts\\\":[],\\\"client_provided_facts\\\":[],\\\"unverified_information\\\":[],\\\"products\\\":[],\\\"services\\\":[],\\\"target_customers\\\":[],\\\"customer_problems\\\":[],\\\"customer_desires\\\":[],\\\"customer_objections\\\":[],\\\"company_differentiators\\\":[],\\\"trust_signals\\\":[],\\\"competitive_context\\\":[],\\\"website_objective\\\":\\\"\\\",\\\"primary_conversion\\\":\\\"\\\",\\\"secondary_conversions\\\":[],\\\"primary_cta\\\":\\\"\\\",\\\"secondary_cta\\\":\\\"\\\",\\\"recommended_sitemap\\\":[],\\\"homepage_conversion_flow\\\":[],\\\"page_requirements\\\":[],\\\"copy_direction\\\":\\\"\\\",\\\"brand_direction\\\":\\\"\\\",\\\"visual_direction\\\":\\\"\\\",\\\"media_requirements\\\":[],\\\"trust_sections\\\":[],\\\"testimonial_requirements\\\":\\\"\\\",\\\"case_study_requirements\\\":\\\"\\\",\\\"faq_direction\\\":\\\"\\\",\\\"form_requirements\\\":[],\\\"whatsapp_requirements\\\":\\\"\\\",\\\"booking_requirements\\\":\\\"\\\",\\\"ecommerce_requirements\\\":\\\"\\\",\\\"crm_opportunities\\\":[],\\\"automation_opportunities\\\":[],\\\"seo_direction\\\":\\\"\\\",\\\"analytics_requirements\\\":[],\\\"mobile_requirements\\\":[],\\\"accessibility_requirements\\\":[],\\\"compliance_considerations\\\":[],\\\"placeholders_required\\\":[],\\\"do_not_invent\\\":[],\\\"questions_for_john\\\":[],\\\"identity_confidence\\\":\\\"high | medium | low\\\",\\\"reasoning\\\":\\\"\\\"}\\n\\nRules for filling it:\\n- Facts only from the digest and John's hand-off. Every claim you cannot point to in the digest goes to `unverified_information` or `placeholders_required` with the words \\\"[CLIENT TO PROVIDE]\\\". Never invent testimonials, awards, certifications, customer counts, years operating, revenue, results, prices, addresses or team members (list them in `do_not_invent`).\\n- `identity_confidence`: high only when the digest says the website was given by John or the customer; medium when search identified it; low when unsure — then use no facts from that site and put the question in `questions_for_john`.\\n- `questions_for_john`: at most 3, only for things research could not answer and that change the design. John decides whether to ask the customer. Do not repeat what the hand-off already answers.\\n- `primary_conversion` is one of: GET QUOTE, WHATSAPP, BOOK APPOINTMENT, BOOK CONSULTATION, REQUEST CALLBACK, SCHEDULE DEMO, BUY NOW, REQUEST PROPERTY VIEWING, VISIT SHOWROOM, CALL NOW, SUBMIT PROJECT DETAILS, BOOK TEST DRIVE, RESERVE A TABLE, BOOK TRIAL CLASS. Pick the one that fits THIS business.\\n- `homepage_conversion_flow`: the adapted journey (hero → value → problem → solution → services → proof → objections → CTA), specific to this business, never \\\"Welcome to ABC\\\".\\n- `form_requirements`: fields that qualify the lead for this business + a PDPA consent line (Singapore).\\n- Singapore context by default. Regulated industries (MOH healthcare/aesthetics, CEA property, MAS financial): list the rule in `compliance_considerations`; never give legal advice.\\n- No prices, packages, guarantees or timelines anywhere. Competitors are context only; never copy them.\\n- `reasoning`: 1–3 sentences for the human reviewer.\\n\";\nconst USER_PROMPT_TEMPLATE = \"Prepare the WEBSITE_CREATOR_BRIEF for this hand-off from John.\\n\\nTenant: {{tenant_id}}\\nLead id: {{lead_id}}\\nNow (UTC): {{now}}\\n\\nJOHN'S HAND-OFF\\n- Customer name: {{contact_name}}\\n- Company: {{company_name}}\\n- Industry: {{industry}}\\n- Location: {{location}}\\n- Website mentioned: {{website}}\\n- Email: {{email}} · Phone: {{phone}} · Channel: {{channel}}\\n- John's summary: {{sales_summary}}\\n- Facts John extracted (JSON): {{extracted_json}}\\n- Conversation (oldest first): \\n{{conversation}}\\n- Current customer message:\\n\\\"\\\"\\\"\\n{{message}}\\n\\\"\\\"\\\"\\n\\nRESEARCH DIGEST (what the search and the public pages returned; labels per Rule 7)\\n{{digest}}\\n\\nReturn the JSON object now.\\n\";\nfunction vaultText(nodeName) {\n  try { const j = $(nodeName).first().json || {}; if (j && j.content && !j.error) { const txt = Buffer.from(String(j.content).replace(/\\n/g, ''), 'base64').toString('utf8'); return txt.replace(/^---[\\s\\S]*?---\\n/, '').replace(/^>.*$/m, '').trim() || null; } } catch (e) {}\n  return null;\n}\nconst d = $('Digest Research').first().json;\nconst input = d.input;\nconst role = vaultText('Load Role Prompt');\nconst system = (role ? role + '\\n\\n' : 'You are the Website Intelligence & Conversion Strategist of FusionTech AI, an internal agent that never talks to the customer.\\n\\n') + ADDENDUM;\nconst convoText = (input.conversation || []).slice(-20).map((m) => '- [' + (m.role || 'customer') + (m.ts ? ' ' + m.ts : '') + '] ' + String(m.content || '').slice(0, 800)).join('\\n') || '(none)';\nconst vars = { tenant_id: input.tenant_id, lead_id: input.lead_id, now: new Date().toISOString(), contact_name: input.contact_name || 'not given', company_name: input.company_name || 'not given', industry: input.industry || 'not given', location: input.location || 'Singapore', website: input.website || 'none mentioned', email: input.email || 'not given', phone: input.phone || 'not given', channel: input.channel, sales_summary: input.sales_summary || '(none)', extracted_json: JSON.stringify(input.extracted || {}), conversation: convoText, message: input.message || '(no message)', digest: d.digest.digest_text };\nconst user_prompt = USER_PROMPT_TEMPLATE.replace(/\\{\\{(\\w+)\\}\\}/g, (_, k) => (k in vars ? String(vars[k]) : ''));\nreturn [{ json: { system_prompt: system, user_prompt, role_source: role ? 'vault' : 'compiled_fallback', config: { model: \"claude-sonnet-4-6\", agent: \"website-intelligence\", agent_version: \"1.0.0\" } } }];\n" }, position: [1980, 300] },
  output: [{ system_prompt: 'FUSION AI — WEBSITE INTELLIGENCE AGENT …', user_prompt: 'Prepare the WEBSITE_CREATOR_BRIEF …', role_source: 'vault', config: { model: "claude-sonnet-4-6", agent: 'website-intelligence', agent_version: '1.0.0' } }]
});

const claude = node({
  type: '@n8n/n8n-nodes-langchain.anthropic',
  version: 1,
  config: {
    name: 'Website Intelligence (Claude)',
    onError: 'continueErrorOutput',
    parameters: { resource: 'text', operation: 'message', modelId: { __rl: true, mode: 'list', value: "claude-sonnet-4-6", cachedResultName: "Claude Sonnet 4.6" }, messages: { values: [{ role: 'user', content: expr("{{ $('Compose Research Prompt').item.json.user_prompt }}") }] }, simplify: true, options: { system: expr("{{ $('Compose Research Prompt').first().json.system_prompt }}"), maxTokens: 4000, temperature: 0.2, includeMergedResponse: true } },
    position: [2200, 300]
  },
  output: [{ text: '{"company_name":"Prestige Motors"}', model: "claude-sonnet-4-6" }]
});

const finalize = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: { name: 'Finalize Brief', parameters: { mode: 'runOnceForAllItems', language: 'javaScript', jsCode: "var WR_VERSION = 'website-intelligence-1.0.0';\nvar WR_SOCIAL_HOSTS = ['facebook.com', 'instagram.com', 'linkedin.com', 'tiktok.com', 'youtube.com', 'x.com', 'twitter.com'];\nvar WR_DIRECTORY_HOSTS = ['google.com', 'maps.google', 'yelp.com', 'tripadvisor', 'wikipedia.org', 'yellowpages', 'sgpbusiness', 'recordowl', 'streetdirectory', 'carousell', 'shopee', 'lazada', 'glassdoor', 'indeed', 'bing.com', 'duckduckgo', 'reddit.com', 'hardwarezone', 'mycareersfuture', 'acra.gov.sg', 'trustpilot', 'sgcarmart'];\nvar WR_INSTRUCTION = 'WEBSITE CREATOR INSTRUCTION\\n\\nUsing the verified business intelligence above, create a premium, modern, mobile-responsive, conversion-focused website mock-up specifically for this company.\\n\\nDo NOT create a generic informational website.\\n\\nThe design and copy structure must be based on:\\n\\n* the company\\'s actual business\\n* its target customers\\n* its services/products\\n* its conversion objective\\n* customer buying motivations\\n* trust requirements\\n* customer objections\\n* the company\\'s brand\\n* the research supplied in this brief\\n\\nThe website must make the visitor understand:\\n\\n1. What this company does.\\n2. Who it helps.\\n3. Why the visitor should care.\\n4. Why the company can be trusted.\\n5. What action the visitor should take next.\\n\\nCreate strong conversion paths through the appropriate combination of:\\n\\n* CTA buttons\\n* WhatsApp\\n* forms\\n* appointments\\n* quotations\\n* consultation requests\\n* calls\\n* purchases\\n\\nDo not fabricate company facts.\\n\\nUse clearly marked placeholders for unavailable content.\\n\\nOnce the mock-up is completed, DO NOT send it to the customer.\\n\\nReturn the completed website/mock-up URL and a short internal summary to:\\n\\nJOHN — FUSION AI SALES AGENT';\nvar WR_VARIATIONS = 'VARIATIONS REQUIRED (Ryan, ADR-2): build TWO versions of this mock-up from the same brief — A: a cinematic scroll film site (Higgsfield animated website; the premium option) and B: a photo-led site on Lovable with generated photography (Higgsfield, Kling fallback). Same pages, copy direction, CTAs and placeholders in both. No prices anywhere; John sends both links.';\nfunction wrStr(v, max) { if (v === undefined || v === null) return ''; var s = String(v).replace(/\\s+/g, ' ').trim(); return max && s.length > max ? s.slice(0, max) : s; }\nfunction wrArr(v, max) { if (!v) return []; if (!Array.isArray(v)) v = [v]; return v.map(function (x) { return typeof x === 'string' ? wrStr(x, 300) : (x && typeof x === 'object' ? wrStr(x.fact || x.text || x.name || JSON.stringify(x), 300) : wrStr(x, 300)); }).filter(Boolean).slice(0, max || 20); }\nfunction wrHost(url) { var m = /^(?:https?:\\/\\/)?(?:www\\.)?([^\\/?#:]+)/i.exec(String(url || '').trim()); return m ? m[1].toLowerCase() : ''; }\nfunction wrUrl(u) { u = wrStr(u, 300); if (!u) return ''; if (!/^https?:\\/\\//i.test(u)) u = 'https://' + u; return u.replace(/[)\\].,;]+$/, ''); }\nfunction wrFindUrls(text) {\n  var out = [], re = /\\b((?:https?:\\/\\/)?(?:www\\.)?[a-z0-9-]+(?:\\.[a-z0-9-]+)*\\.(?:com|sg|co|net|org|io|asia|shop|store|clinic|dental|my|au|uk|biz|info)(?:\\.[a-z]{2})?(?:\\/[^\\s\"'<>)]*)?)/gi, m;\n  text = String(text || '');\n  while ((m = re.exec(text)) !== null) { var u = m[1]; if (/@/.test(text.slice(Math.max(0, m.index - 1), m.index + 1))) continue; if (out.indexOf(u) === -1) out.push(u); }\n  return out;\n}\nfunction wrIsSocial(host) { return WR_SOCIAL_HOSTS.some(function (h) { return host === h || host.endsWith('.' + h); }); }\nfunction wrIsDirectory(host) { return WR_DIRECTORY_HOSTS.some(function (h) { return host.indexOf(h) !== -1; }); }\nfunction wrTokens(s) { return wrStr(s).toLowerCase().replace(/[^a-z0-9 ]/g, ' ').split(' ').filter(function (t) { return t.length > 2 && ['pte', 'ltd', 'llp', 'the', 'and', 'singapore', 'company', 'co', 'inc', 'llc', 'sdn', 'bhd'].indexOf(t) === -1; }); }\n/** John's hand-off → normalized input (same fields as the Website Builder + the website John or the customer mentioned). */\nvar WR_BRIEF_KEYS = ['company_name', 'company_url', 'industry', 'location', 'business_summary', 'verified_facts', 'client_provided_facts', 'unverified_information', 'products', 'services', 'target_customers', 'customer_problems', 'customer_desires', 'customer_objections', 'company_differentiators', 'trust_signals', 'competitive_context', 'website_objective', 'primary_conversion', 'secondary_conversions', 'primary_cta', 'secondary_cta', 'recommended_sitemap', 'homepage_conversion_flow', 'page_requirements', 'copy_direction', 'brand_direction', 'visual_direction', 'media_requirements', 'trust_sections', 'testimonial_requirements', 'case_study_requirements', 'faq_direction', 'form_requirements', 'whatsapp_requirements', 'booking_requirements', 'ecommerce_requirements', 'crm_opportunities', 'automation_opportunities', 'seo_direction', 'analytics_requirements', 'mobile_requirements', 'accessibility_requirements', 'compliance_considerations', 'placeholders_required', 'do_not_invent', 'questions_for_john', 'identity_confidence', 'reasoning'];\nvar WR_LIST_KEYS = ['verified_facts', 'client_provided_facts', 'unverified_information', 'products', 'services', 'target_customers', 'customer_problems', 'customer_desires', 'customer_objections', 'company_differentiators', 'trust_signals', 'competitive_context', 'secondary_conversions', 'recommended_sitemap', 'homepage_conversion_flow', 'page_requirements', 'media_requirements', 'trust_sections', 'form_requirements', 'crm_opportunities', 'automation_opportunities', 'analytics_requirements', 'mobile_requirements', 'accessibility_requirements', 'compliance_considerations', 'placeholders_required', 'do_not_invent', 'questions_for_john'];\nfunction wrConversionFor(industry, signals) {\n  var s = (industry || '').toLowerCase();\n  if (/clinic|dental|aesthetic|medical|doctor|physio|tcm|wellness|spa|salon|beauty|barber/.test(s)) return { primary: 'BOOK APPOINTMENT', secondary: ['WHATSAPP', 'CALL NOW'], cta: 'Book an appointment', cta2: 'WhatsApp us' };\n  if (/property|real estate|agent|realtor/.test(s)) return { primary: 'REQUEST PROPERTY VIEWING', secondary: ['BOOK CONSULTATION', 'WHATSAPP'], cta: 'Request a viewing', cta2: 'Talk to us on WhatsApp' };\n  if (/construction|renovation|contractor|interior|landscap|roofing|plumb|electric|aircon|cleaning|moving|logistic|printing|signage/.test(s)) return { primary: 'GET QUOTE', secondary: ['WHATSAPP', 'CALL NOW'], cta: 'Get a quote', cta2: 'WhatsApp us' };\n  if (/car|auto|motor|dealer|vehicle|bike/.test(s)) return { primary: 'BOOK TEST DRIVE', secondary: ['WHATSAPP', 'VISIT SHOWROOM'], cta: 'Book a test drive', cta2: 'Chat on WhatsApp' };\n  if (/shop|store|retail|e-?commerce|brand|product|fashion|food|bakery|cafe|restaurant|f&b/.test(s)) return { primary: /restaurant|cafe|f&b/.test(s) ? 'RESERVE A TABLE' : 'BUY NOW', secondary: ['WHATSAPP', 'VIEW PRODUCTS'], cta: /restaurant|cafe|f&b/.test(s) ? 'Reserve a table' : 'Shop now', cta2: 'Ask on WhatsApp' };\n  if (/insurance|financial|wealth|advis|account|law|legal|consult|agency|marketing|software|saas|it |tech/.test(s)) return { primary: 'BOOK CONSULTATION', secondary: ['REQUEST CALLBACK', 'WHATSAPP'], cta: 'Book a consultation', cta2: 'Request a callback' };\n  if (/school|tuition|training|course|education|academy/.test(s)) return { primary: 'BOOK TRIAL CLASS', secondary: ['WHATSAPP', 'CALL NOW'], cta: 'Book a trial class', cta2: 'WhatsApp us' };\n  return { primary: signals && signals.shop ? 'BUY NOW' : 'GET QUOTE', secondary: ['WHATSAPP', 'CALL NOW'], cta: signals && signals.shop ? 'Shop now' : 'Get a quote', cta2: 'WhatsApp us' };\n}\n/** Deterministic brief when the model is unavailable: John's facts + what research verified; nothing invented. */\nfunction wrFallbackBrief(input, digest) {\n  var conv = wrConversionFor(input.industry, digest.site.signals);\n  var name = input.company_name || '[CLIENT TO PROVIDE company name]';\n  var verified = digest.facts.filter(function (f) { return /^VERIFIED/.test(f.label); }).map(function (f) { return f.fact; });\n  var provided = digest.facts.filter(function (f) { return f.label === 'JOHN_OR_CUSTOMER_PROVIDED_FACT'; }).map(function (f) { return f.fact; });\n  var third = digest.facts.filter(function (f) { return f.label === 'THIRD_PARTY_PUBLIC_INFORMATION'; }).map(function (f) { return f.fact; });\n  var svc = input.industry ? [input.industry + ' services — [CLIENT TO PROVIDE the exact service list]'] : ['[CLIENT TO PROVIDE services]'];\n  var questions = [];\n  if (digest.identity.confidence === 'low') questions.push('Which website or social page is ' + name + '\\'s official one? (' + digest.identity.reason + ')');\n  if (!input.industry) questions.push('What does ' + name + ' sell or do, exactly?');\n  var said = (input.message + ' ' + input.sales_summary + ' ' + JSON.stringify(input.extracted || {})).toLowerCase();\n  if (!/book|appointment|quote|quotation|buy|order|checkout|whatsapp|enquir|inquir|consult|reserv|test drive|viewing|callback/.test(said)) questions.push('What is the number-one action a visitor should take (quote, booking, WhatsApp, purchase)?');\n  return {\n    company_name: name, company_url: digest.identity.confidence !== 'low' ? digest.identity.website : '', industry: input.industry || '[CLIENT TO PROVIDE]', location: input.location,\n    business_summary: name + (input.industry ? ' is a ' + input.industry + ' business' : '') + ' in ' + input.location + '.' + (input.sales_summary ? ' John: ' + input.sales_summary : '') + (digest.site.description ? ' Site says: ' + digest.site.description : ''),\n    verified_facts: verified, client_provided_facts: provided, unverified_information: third.concat(digest.unknown),\n    products: [], services: svc, target_customers: ['[CLIENT TO PROVIDE] — inferred from the industry: customers in ' + input.location + ' looking for ' + (input.industry || 'this service')],\n    customer_problems: ['Cannot tell quickly what the company does and whether it is trustworthy (INFERENCE)'], customer_desires: ['A clear offer, proof, and one obvious next step (INFERENCE)'], customer_objections: ['Price unclear', 'Is this company legitimate?', 'How fast can they respond?'],\n    company_differentiators: ['[CLIENT TO PROVIDE]'], trust_signals: ['[CLIENT TO PROVIDE] testimonials, certifications, project photos'],\n    competitive_context: digest.competitors.map(function (c) { return c.name + ' (' + c.url + ')'; }),\n    website_objective: 'Turn visitors into ' + conv.primary.toLowerCase() + ' requests', primary_conversion: conv.primary, secondary_conversions: conv.secondary, primary_cta: conv.cta, secondary_cta: conv.cta2,\n    recommended_sitemap: ['Home', 'Services', 'About', 'Projects / Proof [CLIENT TO PROVIDE]', 'FAQ', 'Contact'],\n    homepage_conversion_flow: ['Hero: what we do + for whom + primary CTA', 'Customer problem', 'Solution / services', 'Why choose us', 'Proof [CLIENT TO PROVIDE]', 'FAQ / objections', 'Final CTA + form / WhatsApp'],\n    page_requirements: ['Every page: primary CTA above the fold, WhatsApp button, mobile-first'],\n    copy_direction: 'Plain, specific, outcome-led; speak to ' + (input.industry || 'the customer') + ' buyers in ' + input.location + '; no hype, no invented claims.',\n    brand_direction: '[CLIENT TO PROVIDE brand colours/logo]' + (digest.site.title ? '; current site title: ' + digest.site.title : ''), visual_direction: 'Premium, photo-led, category-appropriate; no generic AI look.',\n    media_requirements: ['Hero photography of the real business [CLIENT TO PROVIDE or generated placeholders]'], trust_sections: ['Testimonials [CLIENT TO PROVIDE]', 'Credentials [CLIENT TO PROVIDE]'],\n    testimonial_requirements: '[CLIENT TESTIMONIALS TO BE ADDED]', case_study_requirements: '[PROJECT IMAGES TO BE PROVIDED]', faq_direction: 'Answer the top buying questions and objections for ' + (input.industry || 'this service'),\n    form_requirements: ['Qualifying fields for a ' + conv.primary.toLowerCase() + ' (name, preferred contact, what they need, when)', 'PDPA consent checkbox + privacy policy link'],\n    whatsapp_requirements: 'Click-to-chat button on every page (number [CLIENT TO PROVIDE])', booking_requirements: conv.primary.indexOf('BOOK') === 0 ? 'Booking request form or calendar link' : 'not required for v1', ecommerce_requirements: conv.primary === 'BUY NOW' ? 'Product catalogue + checkout (Stripe/PayNow)' : 'not required',\n    crm_opportunities: ['Every form/WhatsApp lead into the CRM with source'], automation_opportunities: ['Instant acknowledgement + follow-up task', 'Review request after the job'],\n    seo_direction: (input.industry || 'service') + ' ' + input.location + ' — local intent pages; titles and H1s per page', analytics_requirements: ['GA4 events: cta_click, whatsapp_click, form_submit' + (conv.primary.indexOf('BOOK') === 0 ? ', booking_complete' : '')],\n    mobile_requirements: ['Sticky CTA / WhatsApp on mobile', 'Thumb-friendly buttons', 'Fast images'], accessibility_requirements: ['Readable contrast, alt text, keyboard-friendly forms'],\n    compliance_considerations: /clinic|dental|aesthetic|medical|doctor/i.test(input.industry) ? ['Healthcare advertising rules (MOH) — flag claims for human review'] : (/property|real estate/i.test(input.industry) ? ['CEA advertising rules for property agents'] : (/insurance|financial|wealth/i.test(input.industry) ? ['MAS promotion rules'] : ['PDPA consent on forms'])),\n    placeholders_required: digest.unknown, do_not_invent: ['testimonials', 'awards', 'certifications', 'customer counts', 'years operating', 'revenue', 'results', 'prices', 'addresses', 'team members'],\n    questions_for_john: questions.slice(0, 3), identity_confidence: digest.identity.confidence,\n    reasoning: 'Deterministic brief (model unavailable): built from John\\'s facts, ' + digest.site.pages_read.length + ' page(s) read, ' + digest.competitors.length + ' competitor(s) found.'\n  };\n}\nfunction wrCoerceBrief(raw, fallback) {\n  var b = {}; raw = (raw && typeof raw === 'object') ? raw : {};\n  WR_BRIEF_KEYS.forEach(function (k) {\n    var v = raw[k];\n    if (WR_LIST_KEYS.indexOf(k) !== -1) { v = wrArr(v, 25); if (!v.length && fallback[k] && k !== 'questions_for_john') v = fallback[k]; b[k] = v; }\n    else if (k === 'identity_confidence') b[k] = ['high', 'medium', 'low'].indexOf(v) !== -1 ? v : fallback.identity_confidence;\n    else b[k] = wrStr(v, k === 'business_summary' || k === 'reasoning' ? 1200 : 600) || fallback[k] || '';\n  });\n  return b;\n}\nfunction wrParseJson(text) {\n  if (typeof text !== 'string') return null;\n  var t = text.trim(); var m = /```(?:json)?\\s*([\\s\\S]*?)```/i.exec(t); if (m) t = m[1].trim();\n  var a = t.indexOf('{'), z = t.lastIndexOf('}'); if (a === -1 || z === -1) return null;\n  try { return JSON.parse(t.slice(a, z + 1)); } catch (e) { return null; }\n}\n/** The WEBSITE_CREATOR_BRIEF as text, in Ryan's order, ending with the variations rule and the creator instruction. */\nfunction wrBriefText(brief, digest) {\n  var line = function (k, v) { return k + ':\\n' + (Array.isArray(v) ? (v.length ? v.map(function (x) { return '- ' + x; }).join('\\n') : '- (none)') : (v || '(none)')) + '\\n'; };\n  var order = WR_BRIEF_KEYS.filter(function (k) { return ['questions_for_john', 'identity_confidence', 'reasoning'].indexOf(k) === -1; });\n  return 'STATUS:\\nREADY_FOR_WEBSITE_CREATOR\\n\\nWEBSITE_CREATOR_BRIEF\\n\\n' + order.map(function (k) { return line(k.toUpperCase(), brief[k]); }).join('\\n') + '\\nIDENTITY_CONFIDENCE: ' + brief.identity_confidence + ' (' + digest.identity.reason + ')\\nSOURCES: ' + (digest.site.pages_read.concat(digest.reviews.map(function (r) { return r.source; })).join(', ') || 'John\\'s hand-off only') + '\\n\\n' + WR_VARIATIONS + '\\n\\n' + WR_INSTRUCTION;\n}\n/** Model output (or error) → validated brief + status + text for the Website Creator. Never blocks a mock-up (Rule 12). */\nfunction wrFinalize(o) {\n  var input = o.input, digest = o.digest;\n  var fallback = wrFallbackBrief(input, digest);\n  var provider = 'anthropic', fallbackUsed = false, reason = null, parsed = null;\n  if (o.error) { fallbackUsed = true; reason = wrStr(o.error, 200); }\n  else { parsed = wrParseJson(o.raw_text); if (!parsed) { fallbackUsed = true; reason = 'model_output_not_json'; } }\n  if (fallbackUsed) provider = 'rules';\n  var brief = wrCoerceBrief(parsed || fallback, fallback);\n  if (!parsed) brief = fallback;\n  if (digest.identity.confidence === 'low') { brief.company_url = ''; brief.identity_confidence = 'low'; if (!brief.questions_for_john.length) brief.questions_for_john = fallback.questions_for_john; }\n  var status = input.company_name ? 'READY_FOR_WEBSITE_CREATOR' : 'MORE_INFORMATION_REQUIRED';\n  var text = status === 'READY_FOR_WEBSITE_CREATOR' ? wrBriefText(brief, digest) : 'STATUS:\\nMORE_INFORMATION_REQUIRED\\n\\nQUESTIONS_FOR_JOHN:\\n- What is the company name?\\n\\nWHY_REQUIRED:\\nNo company could be identified from the hand-off.';\n  return { status: status, brief: brief, brief_text: text, questions_for_john: brief.questions_for_john, provider: provider, fallback_used: fallbackUsed, fallback_reason: reason, identity_confidence: brief.identity_confidence, needs_john: brief.questions_for_john.length > 0 };\n}\n// ---- n8n glue ----\nconst d = $('Digest Research').first().json;\nconst pre = $('Compose Research Prompt').first().json;\nconst input = d.input;\nconst inp = ($input.first() && $input.first().json) || {};\nlet rawText = null, error = null, model = pre.config.model;\nif (inp.error) error = 'model_error: ' + String(inp.error.message || inp.error.description || JSON.stringify(inp.error)).slice(0, 300);\nelse {\n  model = inp.model || model;\n  if (typeof inp.text === 'string' && inp.text.trim()) rawText = inp.text;\n  else if (Array.isArray(inp.content)) rawText = inp.content.filter((c) => c && c.type === 'text').map((c) => c.text).join('\\n');\n  else if (typeof inp.output === 'string') rawText = inp.output;\n  if (!rawText) error = 'empty_model_output';\n}\nconst fin = wrFinalize({ raw_text: rawText, error, input, digest: d.digest });\nconst now = new Date().toISOString();\nconst started = d.started_at || now;\nconst researchSlim = { version: d.digest.version, identity: d.identity, facts: d.digest.facts, site: { website: d.digest.site.website, pages_read: d.digest.site.pages_read, pages_failed: d.digest.site.pages_failed, title: d.digest.site.title, description: d.digest.site.description, signals: d.digest.site.signals }, competitors: d.digest.competitors, reviews: d.digest.reviews, unknown: d.digest.unknown, search_items: d.digest.search_items, search_errors: d.digest.search_errors };\nconst eventType = fin.status !== 'READY_FOR_WEBSITE_CREATOR' || fin.needs_john ? 'website.info_needed' : 'website.research';\nreturn [{ json: {\n  input, status: fin.status, ready: fin.status === 'READY_FOR_WEBSITE_CREATOR', needs_john: fin.needs_john, questions_for_john: fin.questions_for_john,\n  brief: fin.brief, brief_text: fin.brief_text, research_json: JSON.stringify(researchSlim), identity_confidence: fin.identity_confidence,\n  provider: fin.provider, model, fallback_used: fin.fallback_used, fallback_reason: fin.fallback_reason, role_source: pre.role_source, config: pre.config,\n  event: { type: eventType, source: 'website-intelligence', tenant_id: input.tenant_id, lead_id: input.lead_id, entity_type: 'lead', entity_id: input.lead_id, severity: eventType === 'website.info_needed' ? 'medium' : 'info', summary: (fin.status === 'READY_FOR_WEBSITE_CREATOR' ? 'Research brief ready for ' + (input.company_name || input.lead_id) + ' (' + fin.identity_confidence + ' identity)' : 'Website Intelligence needs information for ' + input.lead_id) + (fin.questions_for_john.length ? ' — questions for John: ' + fin.questions_for_john.join(' | ') : ''), payload: { questions_for_john: fin.questions_for_john, identity_confidence: fin.identity_confidence, website: d.identity.website, pages_read: researchSlim.site.pages_read, provider: fin.provider }, test_mode: input.test_mode, correlation_id: 'lead:' + input.lead_id },\n  run_id: 'run_' + Date.now().toString(36) + Math.floor(Math.random() * 0xffffff).toString(36), started_at: started, finished_at: now, latency_ms: Math.max(0, new Date(now).getTime() - new Date(started).getTime()),\n  execution_id: String($execution.id), workflow_id: String($workflow.id)\n} }];\n" }, position: [2480, 300] },
  output: [{ input: { tenant_id: 'fusiontech', lead_id: 'lead_x', contact_name: 'Marcus Lim', company_name: 'Prestige Motors', industry: 'car dealership', email: '', phone: '', channel: 'whatsapp', message: 'x', conversation: [], sales_summary: '', extracted: {}, test_mode: true, notify_email: 'owner@example.com', source_execution_id: '1' }, status: 'READY_FOR_WEBSITE_CREATOR', ready: true, needs_john: false, questions_for_john: [], brief: { company_name: 'Prestige Motors', primary_conversion: 'BOOK TEST DRIVE' }, brief_text: 'STATUS: READY_FOR_WEBSITE_CREATOR …', research_json: '{}', identity_confidence: 'high', provider: 'anthropic', model: "claude-sonnet-4-6", fallback_used: false, fallback_reason: null, role_source: 'vault', config: { agent: 'website-intelligence', agent_version: '1.0.0', model: "claude-sonnet-4-6" }, event: { type: 'website.research', source: 'website-intelligence', lead_id: 'lead_x', summary: 'Research brief ready', payload: {}, test_mode: true }, run_id: 'run_x', started_at: '2026-01-01T00:00:00.000Z', finished_at: '2026-01-01T00:00:09.000Z', latency_ms: 9000, execution_id: '1', workflow_id: 'w' }]
});

const logRun = node({
  type: 'n8n-nodes-base.dataTable',
  version: 1.1,
  config: {
    name: 'Log Agent Run',
    executeOnce: true, onError: 'continueRegularOutput',
    parameters: { resource: 'row', operation: 'insert', dataTableId: {"__rl":true,"mode":"id","value":"zoxuUjbgzs6iLIaU","cachedResultName":"ceo_agent_runs"}, columns: { mappingMode: 'defineBelow', value: {
      tenant_id: expr("{{ $('Finalize Brief').first().json.input.tenant_id }}"), run_id: expr("{{ $('Finalize Brief').first().json.run_id }}"), agent: 'website-intelligence', agent_version: "1.0.0", lead_id: expr("{{ $('Finalize Brief').first().json.input.lead_id }}"), workflow_id: expr("{{ $workflow.id }}"), execution_id: expr("{{ $execution.id }}"), model: expr("{{ $('Finalize Brief').first().json.model }}"), provider: expr("{{ $('Finalize Brief').first().json.provider }}"), input_ref: expr("{{ 'lead_intake_execution:' + ($('Finalize Brief').first().json.input.source_execution_id ?? '') }}"), output_json: expr("{{ JSON.stringify({ status: $('Finalize Brief').first().json.status, identity_confidence: $('Finalize Brief').first().json.identity_confidence, questions_for_john: $('Finalize Brief').first().json.questions_for_john, primary_conversion: $('Finalize Brief').first().json.brief.primary_conversion, role_source: $('Finalize Brief').first().json.role_source, research: JSON.parse($('Finalize Brief').first().json.research_json) }) }}"), success: true, error: expr("{{ $('Finalize Brief').first().json.fallback_used ? String($('Finalize Brief').first().json.fallback_reason) : '' }}"), latency_ms: expr("{{ $('Finalize Brief').first().json.latency_ms }}"), test_mode: expr("{{ $('Finalize Brief').first().json.input.test_mode }}"), started_at: expr("{{ $('Finalize Brief').first().json.started_at }}"), finished_at: expr("{{ $('Finalize Brief').first().json.finished_at }}")
    }, schema: [{"id":"tenant_id","displayName":"tenant_id","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"run_id","displayName":"run_id","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"agent","displayName":"agent","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"agent_version","displayName":"agent_version","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"lead_id","displayName":"lead_id","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"workflow_id","displayName":"workflow_id","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"execution_id","displayName":"execution_id","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"model","displayName":"model","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"provider","displayName":"provider","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"input_ref","displayName":"input_ref","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"output_json","displayName":"output_json","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"success","displayName":"success","required":false,"defaultMatch":false,"display":true,"type":"boolean","canBeUsedToMatch":true},{"id":"error","displayName":"error","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"latency_ms","displayName":"latency_ms","required":false,"defaultMatch":false,"display":true,"type":"number","canBeUsedToMatch":true},{"id":"test_mode","displayName":"test_mode","required":false,"defaultMatch":false,"display":true,"type":"boolean","canBeUsedToMatch":true},{"id":"started_at","displayName":"started_at","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"finished_at","displayName":"finished_at","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true}] } },
    position: [2700, 300]
  },
  output: [{ id: 1 }]
});

const logAudit = node({
  type: 'n8n-nodes-base.dataTable',
  version: 1.1,
  config: {
    name: 'Log Audit Trail',
    executeOnce: true, onError: 'continueRegularOutput',
    parameters: { resource: 'row', operation: 'insert', dataTableId: {"__rl":true,"mode":"id","value":"zjeuGe9AEgJS5MKg","cachedResultName":"ceo_audit_logs"}, columns: { mappingMode: 'defineBelow', value: {
      tenant_id: expr("{{ $('Finalize Brief').first().json.input.tenant_id }}"), entity_type: 'lead', entity_id: expr("{{ $('Finalize Brief').first().json.input.lead_id }}"), action: expr("{{ 'website_research_' + $('Finalize Brief').first().json.status.toLowerCase() }}"), old_value: '', new_value: expr("{{ $('Finalize Brief').first().json.identity_confidence + ':' + $('Finalize Brief').first().json.brief.primary_conversion }}"), actor: expr("{{ 'agent:website-intelligence@' + $('Finalize Brief').first().json.provider }}"), execution_id: expr("{{ $execution.id }}"), reason: expr("{{ $('Finalize Brief').first().json.brief.reasoning }}"), ts: expr("{{ $('Finalize Brief').first().json.finished_at }}")
    }, schema: [{"id":"tenant_id","displayName":"tenant_id","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"entity_type","displayName":"entity_type","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"entity_id","displayName":"entity_id","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"action","displayName":"action","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"old_value","displayName":"old_value","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"new_value","displayName":"new_value","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"actor","displayName":"actor","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"execution_id","displayName":"execution_id","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"reason","displayName":"reason","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true},{"id":"ts","displayName":"ts","required":false,"defaultMatch":false,"display":true,"type":"string","canBeUsedToMatch":true}] } },
    position: [2920, 300]
  },
  output: [{ id: 1 }]
});

const readyGate = ifElse({
  version: 2.3,
  config: { name: 'Ready for Creator?', parameters: { conditions: { options: { caseSensitive: true, leftValue: '', typeValidation: 'loose', version: 2 }, conditions: [{ id: 'ready', leftValue: expr("{{ $('Finalize Brief').first().json.ready }}"), rightValue: true, operator: { type: 'boolean', operation: 'true', singleValue: true } }], combinator: 'and' } }, position: [3140, 300] }
});

const handOff = node({
  type: 'n8n-nodes-base.executeWorkflow',
  version: 1.3,
  config: {
    name: 'Hand Off to Website Builder',
    executeOnce: true, onError: 'continueRegularOutput',
    parameters: {
      mode: 'once', source: 'database',
      workflowId: { __rl: true, mode: 'id', value: "hSTRGnHVsu6tMOmH", cachedResultName: 'CEO Brain — Website Builder' },
      workflowInputs: { mappingMode: 'defineBelow', value: { tenant_id: expr("{{ $('Finalize Brief').first().json.input.tenant_id }}"), lead_id: expr("{{ $('Finalize Brief').first().json.input.lead_id }}"), contact_name: expr("{{ $('Finalize Brief').first().json.input.contact_name }}"), company_name: expr("{{ $('Finalize Brief').first().json.input.company_name }}"), industry: expr("{{ $('Finalize Brief').first().json.input.industry }}"), email: expr("{{ $('Finalize Brief').first().json.input.email }}"), phone: expr("{{ $('Finalize Brief').first().json.input.phone }}"), channel: expr("{{ $('Finalize Brief').first().json.input.channel }}"), message: expr("{{ $('Finalize Brief').first().json.input.message }}"), conversation_json: expr("{{ JSON.stringify($('Finalize Brief').first().json.input.conversation ?? []) }}"), sales_summary: expr("{{ $('Finalize Brief').first().json.input.sales_summary }}"), extracted_json: expr("{{ JSON.stringify($('Finalize Brief').first().json.input.extracted ?? {}) }}"), test_mode: expr("{{ $('Finalize Brief').first().json.input.test_mode }}"), notify_email: expr("{{ $('Finalize Brief').first().json.input.notify_email }}"), source_execution_id: expr("{{ $('Finalize Brief').first().json.input.source_execution_id }}"), research_brief: expr("{{ $('Finalize Brief').first().json.brief_text }}"), research_json: expr("{{ $('Finalize Brief').first().json.research_json }}") }, matchingColumns: [], schema: [{"id":"tenant_id","displayName":"tenant_id","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"lead_id","displayName":"lead_id","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"contact_name","displayName":"contact_name","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"company_name","displayName":"company_name","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"industry","displayName":"industry","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"email","displayName":"email","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"phone","displayName":"phone","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"channel","displayName":"channel","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"message","displayName":"message","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"conversation_json","displayName":"conversation_json","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"sales_summary","displayName":"sales_summary","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"extracted_json","displayName":"extracted_json","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"test_mode","displayName":"test_mode","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"boolean"},{"id":"notify_email","displayName":"notify_email","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"source_execution_id","displayName":"source_execution_id","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"research_brief","displayName":"research_brief","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"},{"id":"research_json","displayName":"research_json","required":false,"defaultMatch":false,"display":true,"canBeUsedToMatch":true,"type":"string"}], attemptToConvertTypes: false, convertFieldsToString: true },
      options: { waitForSubWorkflow: false }
    },
    position: [3360, 200]
  },
  output: [{ ok: true }]
});

const notify = node({
  type: 'n8n-nodes-base.httpRequest',
  version: 4.5,
  config: {
    name: 'Tell the Orchestrator',
    executeOnce: true, onError: 'continueRegularOutput',
    parameters: { method: 'POST', url: "https://ryan1515.app.n8n.cloud/webhook/ceo-brain/event", sendBody: true, contentType: 'json', specifyBody: 'json', jsonBody: expr("{{ JSON.stringify($('Finalize Brief').first().json.event) }}"), options: { timeout: 20000 } },
    position: [3580, 300]
  },
  output: [{ ok: true }]
});

const note = sticky("## CEO Brain — Website Intelligence (internal agent, ADR-3)\nCalled by Lead Intake with John's hand-off (same inputs as the Website Builder). Never talks to the customer.\n\nFlow: plan 6 queries → Web Search (Browserbase, n8n gateway credits) → identify the company (John's/customer's URL wins; else best match; low confidence = no site facts) → fetch homepage + up to 3 pages → digest with a labelled fact ledger (VERIFIED_PUBLIC_FACT / JOHN_OR_CUSTOMER_PROVIDED_FACT / THIRD_PARTY_PUBLIC_INFORMATION / INFERENCE / UNKNOWN) → Ryan's role prompt live from the vault → Claude (deterministic fallback when the model is unavailable) → WEBSITE_CREATOR_BRIEF (JSON + text, two variations, placeholders, creator instruction) → run + audit → Hand Off to Website Builder (research_brief, research_json) → event to the Orchestrator (website.research, or website.info_needed with questions for John).\n\nRule 12: a mock-up is never delayed for missing information. Source of truth: ryan/ceo-brain/workflows/website-intelligence/build.js — do not hand-edit Code nodes.", [whenCalled, plan, search, identify], { color: 4 });

export default workflow('ceo-brain-website-intelligence', 'CEO Brain — Website Intelligence')
  .add(whenCalled)
  .to(plan)
  .to(search)
  .to(identify)
  .to(fetchHome)
  .to(pickPages)
  .to(fetchPages)
  .to(digest)
  .to(loadRole)
  .to(compose)
  .to(claude.to(finalize))
  .add(claude.onError(finalize))
  .add(finalize)
  .to(logRun)
  .to(logAudit)
  .to(readyGate
    .onTrue(handOff.to(notify))
    .onFalse(notify))
  .add(note);
