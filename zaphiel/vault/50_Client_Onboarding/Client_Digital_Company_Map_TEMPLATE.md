---
type: template
priority: P0
status: live
tags: [onboarding, company-map, template]
---
# Client Digital Company Map — template

The template used by the live Discovery Agent is **[[Templates/Client Digital Company Map]]** (kept where n8n expects it). This note exists so the blueprint path resolves; do not duplicate the content.

A finished map is stored per client in `80_Clients/<client>/Company_Map.md` (see [[80_Clients/_TEMPLATE_Client/README]]), never mixed with another client. Test maps live in `Discovery/Test/`.
